apps:
  - name: "messaging-service"
    script: "dist/main.js"
    instances: 1
    autorestart: true
    watch: true  # Enable watch mode
    max_memory_restart: 1G
    env:
      NODE_ENV: "production"